# 音乐作品重开验证必须等待实际完成

2026-09-10，组件交付的强制 `large_soundtrack_studio_gpu` 回归失败，
见 `out/p7-input-components-final-delivery.log`，原始失败保留。
增加阶段诊断后，`out/p7-soundtrack-diagnostics.log` 中同源的
`time_section_studio_gpu` 在第 361 帧提前结束：保存、发布、清除均成立，
但恢复音频为 false、波形为 0。旧检查只有“流程未完成”的总错误。

测试原来在发布后固定第 20 帧点击重开、第 30 帧展开时间线、第 221 帧结束。
项目读取、资产校验和波形扫描是异步任务，固定帧数不是这些操作完成的合同。
旧 `HasValidPlan()` 也没有证明重开请求已经被接受；请求被禁用按钮忽略时，
它仍可能对应清除音乐后的图。这是测试同步与证据缺口，不能由本次结果推断
产品的后台加载一定存在错误，也不能用重跑偶然通过覆盖失败。

修复验证入口 `src/windows_spike/tests/soundtrack_studio_gpu_tests.cpp`：

- 清除后等待当前计划有效，记录重开前编译代数。
- 尚未出现新请求时允许再次点击重开；出现新请求后停止重复操作。
- 只有新代数的计划安装成功，才检查重开后的音频和展开时间线。
- 音频特征与实际波形都恢复后截图，继续提交帧以完成截图回调。
- 保留 40 秒墙钟上限；失败报告保存、发布、清除、恢复、计划、节点数、
  波形与重开帧，不改音频/波形/包身份的合格条件。

`out/p7-soundtrack-state-regressions.log`：8 条实际 Studio 音乐工作流及
3 个输入 fixture 全部通过，包含大 WAV、时间段、原有编排替换、和声城市，
以及 Ink/Loom/Porcelain/Arcade 四个校准作品的替换保存发布重开。
最后交付回归 `out/p7-input-components-current-delivery.log`：9 项全部通过，
75.01 秒，包含修订后的重开请求去重条件与大音乐真实工作流。

这是实际编辑器工作流检查；不等同于声学回录、听感或长时间稳定性验收。
