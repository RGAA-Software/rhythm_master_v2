# 统一 GPU 粒子光晕与发布宿主同步（2026-09-11）

## 用户报告、根因和缺失路径

用户在 `星铸圣坛 / Astral Forge` 中看到蓝色粒子似乎被重复拖影，并指出把粒子单纯缩小不能解决问题：大粒子也必须保持自然、连续的柔光边缘。

根因有两层：

- 作品把多个青色轨道航标作为不透明 `scene.instance` 对象绘制，又对最终画面做 `texture.blur` 叠加。模糊制造了蓝色重影；移除模糊以后，拉长球体的硬几何轮廓仍然不适合作为大型能量粒子。
- 通用 GPU 点精灵只有尺寸参数。此前若想扩大光晕，只能在精灵外增加纹理模糊，导致不同尺寸和不同模板出现不一致的光斑、拖影与重影。
- 初版通用点精灵虽使用解析亮核，但只在半径最后 22% 执行透明衰减。小点不明显，大点会显出圆片状硬边。原回归只比较中心、中部和边缘亮度，没有检查扩展光晕中多个连续采样点，因而漏掉视觉上的陡峭边界。

初次修复后，Studio 交付的模板应用检查通过，但目录缩略图和音乐质量脚本读取当前包时报告 `package.invalid_program`。包本身有效；`windows_effects_gpu_tests.exe` 和 `music_gpu_tests.exe` 是接口更新前留下的可执行程序，未被 Studio delivery 的目标集重建。这是验证宿主版本漂移，不是包或图的错误。

## 统一修复

- `GpuPointStyle` 增加有界的 `glow_radius_`，公开节点属性为 `gpu.render.point_glow_radius`，范围 1–4。
- 顶点着色器只扩大同一颗点精灵的覆盖范围；片段着色器在这个范围内计算高斯亮核和局部光晕，并把透明衰减范围扩大到半径的 80%。核心尺寸保持稳定，光晕随半径连续扩展，不再使用整屏或粒子纹理模糊。
- GPU 回归现在对扩展光晕的内、中、外和边缘四个位置检查严格递减，永久阻止硬边回归。
- `星铸圣坛` 删除最终整画面 `texture.blur`。14 个青色航标从不透明 3D 球体迁移到相同的解析柔光粒子路径，保留大尺寸、持续旋转和低/中/高频响应；初始为空，以每秒速率逐渐补充，高频和中频提升生成率，不会整批喷出。加上青色尘光和金色火花，作品现在有三层独立 GPU 粒子。
- `tools/build-windows.py` 的每次 Studio delivery 现在同时构建 `windows_effects_gpu_tests` 与 `music_gpu_tests`，使目录缩略图和解码 PCM 验收使用与发布器相同的运行时包读取器。

## 当前 Windows 证据

- `out/soft-edge-gpu.log.runs/1789103626939739000.log`：真实 D3D11 点精灵回归通过，包括扩展光晕的四段连续递减和 10K/100K/262144 粒子规模。
- `out/astral-forge-soft-beacons-final.log.runs/1789103940354999300.log`：从当前运行时包渲染成功；输出在 `out/catalog-thumbnails/5e3f74cc97fe49a0be84a09b0d22b4cd/astral_forge/`。人工查看确认大号青色粒子保持可见尺寸，边缘连续柔和，没有蓝色重复轮廓。
- `out/windows-release/studio-delivery-tests.log.runs/1789104646003331400.log`：Studio 实际模板套用、节点 ID 重映射、当前输出、保存/重开和发布路径 9/9 通过；Studio 和 Player deploy 均含 20 个 DLL、可执行文件与资源。
- `out/astral-forge-soft-particles-quality.log.runs/1789104939204842100.log`：198 个可达指令、63 个频段、三层 GPU 粒子。低/静音 1.7665，高/静音 4.6467，中/静音 4.4178，强弱音乐与各频段比较均超过阈值。
- `out/astral-forge-soft-particles-controls.log.runs/1789104965348024400.log`：六个公开控制项的最小/最大图像差为 2.1634–26.5374，全部超过阈值。

以后任何更改 GPU 点样式、节点描述或内容打包器的提交都必须：重建这两个独立 GPU 宿主、从当前运行时包实际渲染缩略图、再运行解码 PCM 质量检查。静态图、旧测试可执行程序或只验证包发布器都不能替代这些路径。
