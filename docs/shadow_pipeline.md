# 基础阴影（R3）

`scene.shadow` 为输入场景配置一个投影光源。方向光使用正交阴影体，并可选择单图或
双级联；聚光使用该灯位置、朝向和光锥建立透视投影；点光按 Godot cube 模式使用
+X、-X、-Y、+Y、+Z、-Z 六个 90 度透视面建立全向阴影。
光源索引先方向光、再局部光，从 0 开始。场景合并调整索引以保留被选中的光源；
两个含阴影设置的场景合并会报错，避免静默丢弃其中一个配置。

## 控件与预算

阴影图为 256/512/1024/2048 四档，默认 1024。滤波提供 Nearest、PCF5（低）和
PCF13（高）三档，旧作品的数值 `0/1` 分别保持 Nearest/PCF5，默认仍为 PCF5。
深度偏移和法线偏移保持独立可调。
方向光可调世界空间中心、宽度和距离；聚光和点光近裁剪面单独配置，远面使用灯光作用范围。
近面必须小于远面；极小近面会损失远处深度精度，不隐式替用户改变这个参数。
双级联方向光按接收相机 view depth 分割，默认 split 为 0.25、最远距离为 40 世界单位；
旧图和默认属性仍使用原单图 center/extent 合同。聚光和点光不允许启用级联。
场景变换更新阴影中心，范围和偏移保持世界单位；聚光相机始终跟随变换后的灯光。

每次需要更新的阴影增加一次已有场景深度 pass，只让不透明几何投影，保留网格复用、
剔除和批处理。接收场景只对选中灯光乘遮挡，其他灯、自发光保持原行为。
深度/颜色附件均由 Runtime 的独立 ShadowPass RAII 持有，每级合计每像素 8 字节，
1024 档单图为 8 MiB、双级联为 16 MiB、点光六面为 48 MiB，纳入已有纹理总预算。
图编译的三角形工作预算按单图/级联/点光实际需要的 1/2/6 个投影 pass 计算；因此原本
能使用方向光阴影的高几何作品不保证能在同一预算内切换为点光阴影。
设置不变且上游没有更新时复用结果；分辨率变化替换两附件，关闭/隐藏预览/重置释放。
新旧阴影图切换时短暂同时计入预算，分配失败不发布半个附件对。

## 坐标与来源

输入保持项目标准右手矩阵和 [-1,+1] clip 深度，接收 shader 映射到 [0,1] 深度
与左上 UV。域外采样返回完全照亮，禁止深度读写同一附件。
PCF5、PCF13 滤波核和几何法线偏移来自固定 Godot 4.5.1 GLES 源码（MIT），见
`provenance/godot_shadows.json`。采用已验证 D24S8 原始深度采样与显式比较，
PCF13 保留中心、远十字、近十字和对角线共 13 个比较，并保留 Godot 在前五个
比较全亮或全暗时的提前返回。双级联进一步采用同版本 Godot 的相机视锥分段、八端点
包围球、单 texel 扩展和每级稳定吸附；项目用两张有界 2D 深度图代替全局 atlas。
点光进一步采用 `renderer_scene_cull.cpp` 的 cube face 顺序、方向、up 向量、90 度投影和
灯光 range 远面。现有公开渲染合同没有原生 cube render-target face，因此以六张有界
2D 深度图适配，接收端按灯光相对向量主轴选择同序面。没有引入第二个渲染后端、
四级联或 split blending。

方向光投影还采用同一固定 Godot 版本的稳定边界算法。Godot 以
`radius * 4 / texture_size` 吸附正交边界；项目合同保存完整投影高度，因此等价为让
投影中心在光空间横向和纵向按两个阴影 texel 移动。小于半格的中心运动保持同一矩阵，
越过边界才整格前进；沿光照深度的运动不量化，聚光透视投影也不量化。

## 短验证与作品

Windows D3D11 和 USB Adreno 650/GLES 3.1 已通过正交/透视聚光遮挡、UV 朝向、
选中灯光、域外照亮、深度偏移以及关闭绑定检查。Null 契约拒绝颜色误绑、
同深度读写、分辨率不匹配、不存在的灯、投影类型与灯类型不一致、点光缺面/重复面
和非有限参数。
Runtime 两端通过合并光源身份、缓存、预览、分辨率替换和释放检查。

2026-09-16 的 Windows 增量进一步用同一 256 阴影图和 128×128 接收画面分别回读
Nearest、PCF5、PCF13；红色直射光通道的全图差异依次为 1920 和 1792，证明两次
质量升级都实际改变投影边缘；中间强度像素数依次为 0、24、210，证明高档扩大了平滑
过渡，而不是只改变枚举。D3D11 完整 GPU probe、三项受影响
合同测试和 Windows/Android shader 编译通过，详见
[验证记录](validation/godot_pcf13_shadows_2026-09-16.md)。

音律珐琅更新为 61 指令/79 边，增加聚光阴影与承影舞台。真实 PCM 音乐/静音
平均 RGB 差异 3.270305，低频/静音 10.883352，高频/静音 5.440631，
低频/高频 10.089402。重复 MP4 导出、音轨、时间戳、静音和取消检查通过。
窗口与 APK 已构建并完整部署/覆盖安装，35 个内置示例保持。
手机已从 UI 选择新包并完成 16 秒演示音乐，所选包哈希与 APK/Windows 相同。
截图 `out/r3-android-shadows-music.png`；产物哈希 `out/r3-shadows-delivery.json`。

当前没有透明物体投影；方向光已提供兼容默认单图和可选双级联，点光提供六面全向阴影。稳定
方向光投影已有矩阵合同，并以实际
D3D11 两帧回读确认子网格移动输出不变、未吸附对照会改变像素。Porcelain Bloom 又以
当前编译作者图完成静音四秒移动相机回读：相机移动 3.277 世界单位，连续帧 RGB 差异
p50/p95/max 为 4.419/5.954/6.100，未见突发跳变，阴影对照和纹理稳定性均通过。
Chromatic Loom 又完成 481 帧细几何检查：PCF5→PCF13 有实际像素差，方向光范围从 9
扩大到 36 后阴影仍可见，16 秒边界变化低于普通帧中位数。随后双级联 PCF13 从属性、
预算、运行时相机和两张深度图贯通到同一作品的 D3D11 输出；相对无阴影和单图的最大
平均 RGB 差分别为 0.362297 和 0.359650，额外纹理恰为 8 MiB。随后 Sonic Enamel
使用作品里已有的点光（light 1）完成 121 帧全向 PCF13：相对关闭阴影的最大平均 RGB
差为 0.308220，稳定纹理由 20,053,444 增至 70,385,092 字节，差值恰为六个 1024²
颜色/深度对的 48 MiB。点光 p50/p95 短 host-frame 为 9.960/13.874 ms，无阴影为
5.984/11.349 ms；这里只记录整帧短测，不宣称隔离 GPU pass 成本。

当前六张 2D 图的 PCF tap 在所选面内 clamp，不具备 Godot `samplerCubeShadow` 的跨面
滤波；cube 边缘 seam、Android 实机像素、更多代表作品、多个完整作品周期和隔离 GPU
成本仍待后续 W1.1 增量。本次不把聚焦短测等同完整视觉品质验收。长稳保持最后集中执行。
实现、来源和逐项证据见[点光验证记录](validation/godot_point_shadows_2026-09-16.md)。
