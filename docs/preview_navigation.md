# R6 多节点预览分组

沿用每帧最多 8 个预览、256×144、15 Hz 的已验证预算，
增加明确的前后组操作和页码。不会把每个可见节点同时按最终输出尺寸渲染。

主图与当前组件内部图的可见需求交替排列，单次只编译当前组的最多 8 个预览入口。
两边都有需求时首组各获得名额；切换组可遍历全部可见需求，不再只截取前 8 个。
选择节点仍提升其在本作用域内的优先级；需求或作用域变更返回首组，页码不会越界。
编译和资源准备完成后才切换实际画面，沿用迟到结果丢弃和 GPU 资源预算。

分组不会修改工程或作品时钟。最终输出依赖链持续运行；纯预览支路离组后释放状态，
再次进入从当时时间开始建立其局部历史，不承诺重建隐藏粒子/反馈的过去模拟。
本批采用用户主动切换，避免后台自动轮换不停触发编译或重建预览支路。

复用现有 PreviewRouting、CompilerWorker、Viewers 和组件作用域映射，
不增加后台服务或图内设备类型。验证覆盖分页可达性、双作用域分配、选择/作用域变化、
迟到编译防护及真实大图的最终输出连续性。

## 功能证据

- `preview_routing`：17 个主图需求 + 9 个组件需求全部可遍历，同值 ID 按作用域区分，
  每组不超过 8 个，首组各 4 个；选择、实例路径和工程改变复位，空需求和去重通过。
- `component_interactions`：修改了原“组件优先占满”断言以对应交替分配；
  保留提交前不发布映射、作用域改变清除旧映射与草稿隔离验证。
- 组件中英文时间 UI、真实组件预览及时间编辑 GPU 回归通过。
- `preview_groups_gpu`：40 节点工程实际访问 5 组、总预算 8，工程保持不变；
  60 帧中反复重编译预览入口，最终拖尾经显示转换后的非黑 GPU 像素
  与从未切组的独立运行时完全一致。此夹具验证导航和状态，不算品质模板。

本地截图 `out/r6-preview-groups.png`；最终聚焦记录 `out/r6-preview-delivery-tests.log`。
本批 10 项聚焦检查全部通过，Windows Studio/Player deploy 已按脚本同步。
改动属于 Studio 创作 UI，Android Player 没有新增编辑入口；继续沿用已覆盖安装的播放器。
纯预览状态恢复限制见上文；没有把主动分页宣称为无限同时预览。
