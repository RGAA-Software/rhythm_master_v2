* text=auto

# Imported files retain the exact upstream bytes recorded in provenance.
third_party/** -text
dependencies/vcpkg/ports/**/*.patch -text
dependencies/vcpkg/ports/**/*.diff -text
*.rgba binary

# A content-addressed asset's identity includes every byte, including line endings.
**/assets/sha256/** -text
