add_library(audio_playback STATIC playback.cpp playback_engine.cpp analysis_queue.cpp stream_source.cpp transition_stream.cpp playback_presentation.cpp)
target_include_directories(audio_playback PUBLIC include)
target_link_libraries(audio_playback PUBLIC audio_types media_arrangement PRIVATE media_mixer audio_output audio_analysis)
rhythm_project_target(audio_playback)
if(BUILD_TESTING)
    add_executable(playback_engine_tests tests/playback_engine_tests.cpp)
    target_include_directories(playback_engine_tests PRIVATE .)
    target_link_libraries(playback_engine_tests PRIVATE audio_playback media_mixer audio_output audio_analysis)
    rhythm_project_target(playback_engine_tests)
    add_test(NAME playback_engine COMMAND playback_engine_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests")
    set_tests_properties(playback_engine PROPERTIES FIXTURES_REQUIRED local_audio_media TIMEOUT 30)
    if(ANDROID)
        target_sources(playback_engine_tests PRIVATE ../audio_output/tests/audio_test_host.cpp)
        target_include_directories(playback_engine_tests PRIVATE ../audio_output/tests)
        target_link_libraries(playback_engine_tests PRIVATE SDL3::SDL3)
    endif()
    if(WIN32)
        set_tests_properties(playback_engine PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
    add_executable(playback_presentation_tests tests/playback_presentation_tests.cpp playback_presentation.cpp analysis_queue.cpp)
    target_include_directories(playback_presentation_tests PRIVATE .)
    target_link_libraries(playback_presentation_tests PRIVATE media_mixer audio_analysis)
    rhythm_project_target(playback_presentation_tests)
    add_test(NAME playback_presentation COMMAND playback_presentation_tests)
    if(WIN32)
        set_tests_properties(playback_presentation PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
    add_executable(transition_stream_tests tests/transition_stream_tests.cpp stream_source.cpp transition_stream.cpp)
    target_include_directories(transition_stream_tests PRIVATE .)
    target_link_libraries(transition_stream_tests PRIVATE media_mixer)
    rhythm_project_target(transition_stream_tests)
    add_test(NAME transition_stream COMMAND transition_stream_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests")
    set_tests_properties(transition_stream PROPERTIES FIXTURES_REQUIRED local_audio_media TIMEOUT 60)
    if(WIN32)
        set_tests_properties(transition_stream PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
    add_executable(playback_analysis_tests tests/analysis_queue_tests.cpp analysis_queue.cpp)
    target_include_directories(playback_analysis_tests PRIVATE .)
    target_link_libraries(playback_analysis_tests PRIVATE media_audio audio_analysis)
    rhythm_project_target(playback_analysis_tests)
    add_test(NAME playback_analysis COMMAND playback_analysis_tests)
    if(WIN32)
        set_tests_properties(playback_analysis PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
    add_executable(audio_playback_tests tests/playback_tests.cpp)
    if(ANDROID)
        target_sources(audio_playback_tests PRIVATE ../audio_output/tests/audio_test_host.cpp)
        target_include_directories(audio_playback_tests PRIVATE ../audio_output/tests)
        target_link_libraries(audio_playback_tests PRIVATE SDL3::SDL3)
    endif()
    target_link_libraries(audio_playback_tests PRIVATE audio_playback atomic_storage)
    rhythm_project_target(audio_playback_tests)
    if(WIN32)
        add_test(NAME audio_playback COMMAND audio_playback_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests")
        set_tests_properties(audio_playback PROPERTIES FIXTURES_REQUIRED local_audio_media TIMEOUT 60
            ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
endif()
