add_library(scene3d STATIC math.cpp camera.cpp model.cpp sphere.cpp torus.cpp resources.cpp tangents.cpp path.cpp tube.cpp animation.cpp model_animation.cpp skinning.cpp morph.cpp)
target_link_libraries(scene3d PUBLIC asset_types)
target_sources(scene3d PRIVATE pose.cpp picking.cpp)
target_include_directories(scene3d PUBLIC include)
if(ANDROID)
    set(rhythm_math_triplet arm64-android)
else()
    set(rhythm_math_triplet x64-windows)
endif()
find_path(RHYTHM_GLM_INCLUDE glm/glm.hpp PATHS "C:/source/vcpkg/installed/${rhythm_math_triplet}/include"
    NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH REQUIRED)
target_include_directories(scene3d SYSTEM PRIVATE "${RHYTHM_GLM_INCLUDE}")
find_package(mikktspace CONFIG REQUIRED PATHS
    "C:/source/vcpkg/installed/${rhythm_math_triplet}/share/mikktspace"
    NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH)
target_link_libraries(scene3d PRIVATE mikktspace::mikktspace)
rhythm_project_target(scene3d)
if(BUILD_TESTING)
    add_executable(scene_picking_tests tests/picking_tests.cpp)
    target_link_libraries(scene_picking_tests PRIVATE scene3d)
    rhythm_project_target(scene_picking_tests)
    add_test(NAME scene_picking COMMAND scene_picking_tests)
    add_executable(scene_pose_tests tests/pose_tests.cpp)
    target_link_libraries(scene_pose_tests PRIVATE scene3d)
    rhythm_project_target(scene_pose_tests)
    add_test(NAME scene_pose COMMAND scene_pose_tests)
    add_executable(scene_skinning_tests tests/skinning_tests.cpp)
    target_link_libraries(scene_skinning_tests PRIVATE scene3d)
    rhythm_project_target(scene_skinning_tests)
    add_test(NAME scene_skinning COMMAND scene_skinning_tests)
    add_executable(scene_animation_tests tests/animation_tests.cpp)
    target_link_libraries(scene_animation_tests PRIVATE scene3d)
    rhythm_project_target(scene_animation_tests)
    add_test(NAME scene_animation COMMAND scene_animation_tests)
    add_executable(scene_path_tests tests/path_tests.cpp)
    target_link_libraries(scene_path_tests PRIVATE scene3d)
    rhythm_project_target(scene_path_tests)
    add_test(NAME scene_paths COMMAND scene_path_tests)
    add_executable(scene_tangent_tests tests/tangent_tests.cpp)
    target_link_libraries(scene_tangent_tests PRIVATE scene3d)
    rhythm_project_target(scene_tangent_tests)
    add_test(NAME scene_tangents COMMAND scene_tangent_tests)
    add_executable(scene_math_tests tests/math_tests.cpp)
    target_link_libraries(scene_math_tests PRIVATE scene3d)
    rhythm_project_target(scene_math_tests)
    add_test(NAME scene_math COMMAND scene_math_tests)
    add_executable(scene_primitive_tests tests/primitive_tests.cpp)
    target_link_libraries(scene_primitive_tests PRIVATE scene3d)
    rhythm_project_target(scene_primitive_tests)
    add_test(NAME scene_primitives COMMAND scene_primitive_tests)
endif()
