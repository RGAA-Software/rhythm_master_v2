add_library(cgltf_backend STATIC cgltf_impl.c)
target_include_directories(cgltf_backend SYSTEM PUBLIC "${RHYTHM_CGLTF_INCLUDE}")
add_library(model_import STATIC gltf_import.cpp gltf_validation.cpp gltf_mesh.cpp gltf_images.cpp)
target_include_directories(model_import PUBLIC include)
target_link_libraries(model_import PUBLIC scene3d PRIVATE cgltf_backend)
if(RHYTHM_BUILD_MEDIA)
    target_link_libraries(model_import PRIVATE media_video)
    target_compile_definitions(model_import PRIVATE RHYTHM_HAS_IMAGE_DECODER=1)
endif()
rhythm_project_target(model_import)
if(BUILD_TESTING)
    add_executable(model_import_tests tests/import_tests.cpp)
    target_link_libraries(model_import_tests PRIVATE model_import)
    rhythm_project_target(model_import_tests)
    add_test(NAME model_import COMMAND model_import_tests)
    if(RHYTHM_BUILD_MEDIA)
        target_compile_definitions(model_import_tests PRIVATE RHYTHM_TEST_IMAGE_DECODER=1)
        if(WIN32)
            set_tests_properties(model_import PROPERTIES ENVIRONMENT_MODIFICATION
                "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin;PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/debug/bin")
        endif()
    endif()
endif()
