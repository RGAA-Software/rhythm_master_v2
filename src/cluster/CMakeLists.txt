add_library(cluster_time STATIC clock.cpp)
target_include_directories(cluster_time PUBLIC include)
rhythm_project_target(cluster_time)
add_library(cluster_inputs STATIC input_buffer.cpp)
target_include_directories(cluster_inputs PUBLIC include)
target_link_libraries(cluster_inputs PUBLIC runtime_inputs)
rhythm_project_target(cluster_inputs)
add_library(cluster_protocol STATIC datagram_codec.cpp clock_exchange.cpp)
target_include_directories(cluster_protocol PUBLIC include)
target_link_libraries(cluster_protocol PUBLIC runtime_inputs)
rhythm_project_target(cluster_protocol)
add_library(cluster_send_queue STATIC send_queue.cpp stream_decoder.cpp)
target_include_directories(cluster_send_queue PUBLIC include)
rhythm_project_target(cluster_send_queue)
add_library(cluster_scene STATIC scene_coordinator.cpp)
target_include_directories(cluster_scene PUBLIC include)
rhythm_project_target(cluster_scene)
if(BUILD_TESTING)
    add_executable(scene_contract_tests tests/scene_contract_tests.cpp)
    target_link_libraries(scene_contract_tests PRIVATE cluster_scene)
    rhythm_project_target(scene_contract_tests)
    add_test(NAME scene_contracts COMMAND scene_contract_tests)
    add_executable(stream_contract_tests tests/stream_contract_tests.cpp)
    target_link_libraries(stream_contract_tests PRIVATE cluster_send_queue)
    rhythm_project_target(stream_contract_tests)
    add_test(NAME stream_contracts COMMAND stream_contract_tests)
    add_executable(send_queue_contract_tests tests/send_queue_contract_tests.cpp)
    target_link_libraries(send_queue_contract_tests PRIVATE cluster_send_queue)
    rhythm_project_target(send_queue_contract_tests)
    add_test(NAME send_queue_contracts COMMAND send_queue_contract_tests)
    add_executable(exchange_contract_tests tests/exchange_contract_tests.cpp)
    target_link_libraries(exchange_contract_tests PRIVATE cluster_protocol cluster_time)
    rhythm_project_target(exchange_contract_tests)
    add_test(NAME exchange_contracts COMMAND exchange_contract_tests)
    add_executable(datagram_contract_tests tests/datagram_contract_tests.cpp)
    target_link_libraries(datagram_contract_tests PRIVATE cluster_protocol cluster_inputs)
    rhythm_project_target(datagram_contract_tests)
    add_test(NAME datagram_contracts COMMAND datagram_contract_tests)
    add_executable(input_contract_tests tests/input_contract_tests.cpp)
    target_link_libraries(input_contract_tests PRIVATE cluster_inputs)
    rhythm_project_target(input_contract_tests)
    add_test(NAME input_contracts COMMAND input_contract_tests)
    add_executable(clock_contract_tests tests/clock_contract_tests.cpp)
    target_link_libraries(clock_contract_tests PRIVATE cluster_time)
    rhythm_project_target(clock_contract_tests)
    add_test(NAME clock_contracts COMMAND clock_contract_tests)
endif()
