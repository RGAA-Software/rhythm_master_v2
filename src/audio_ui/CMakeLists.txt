add_library(audio_ui STATIC audio_panel.cpp)
target_include_directories(audio_ui PUBLIC include)
target_link_libraries(audio_ui PUBLIC audio_capture runtime_inputs PRIVATE spike_imgui)
if(RHYTHM_BUILD_MEDIA)
    target_compile_definitions(audio_ui PUBLIC RHYTHM_HAS_LOCAL_MEDIA=1)
    target_link_libraries(audio_ui PUBLIC audio_playback media_types)
    target_link_libraries(audio_ui PRIVATE media_mixer)
endif()
rhythm_project_target(audio_ui)
if(BUILD_TESTING AND RHYTHM_BUILD_MEDIA AND WIN32)
    add_executable(arrangement_host_tests tests/arrangement_host_tests.cpp)
    target_link_libraries(arrangement_host_tests PRIVATE audio_ui media_mixer)
    rhythm_project_target(arrangement_host_tests)
    add_test(NAME arrangement_host COMMAND arrangement_host_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests/tone.flac")
    set_tests_properties(arrangement_host PROPERTIES FIXTURES_REQUIRED local_audio_media TIMEOUT 30
        ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin;PATH=path_list_prepend:${RHYTHM_SPIKE_SDK}/bin")
endif()
