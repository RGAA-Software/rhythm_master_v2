add_library(audio_types INTERFACE)
target_include_directories(audio_types INTERFACE include)
add_library(audio_analysis STATIC analyzer.cpp detail/fft.cpp detail/band_map.cpp detail/onset.cpp detail/tempo.cpp)
target_link_libraries(audio_analysis PUBLIC audio_types)
rhythm_project_target(audio_analysis)
if(BUILD_TESTING)
    add_executable(audio_analysis_tests tests/analysis_tests.cpp)
    target_include_directories(audio_analysis_tests PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
    target_link_libraries(audio_analysis_tests PRIVATE audio_analysis)
    rhythm_project_target(audio_analysis_tests)
    add_test(NAME audio_analysis COMMAND audio_analysis_tests)
endif()
