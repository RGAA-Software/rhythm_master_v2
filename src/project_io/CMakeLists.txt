set(schema "${CMAKE_CURRENT_SOURCE_DIR}/schema/graph.proto")
add_custom_command(OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/graph.pb.cc" "${CMAKE_CURRENT_BINARY_DIR}/graph.pb.h"
    COMMAND "${RHYTHM_PROTOC}" --cpp_out "${CMAKE_CURRENT_BINARY_DIR}"
        --proto_path "${CMAKE_CURRENT_SOURCE_DIR}/schema" "${schema}"
    DEPENDS "${schema}" VERBATIM)
add_library(project_schema STATIC "${CMAKE_CURRENT_BINARY_DIR}/graph.pb.cc")
target_link_libraries(project_schema PRIVATE protobuf::libprotobuf)
target_include_directories(project_schema SYSTEM PUBLIC "${CMAKE_CURRENT_BINARY_DIR}")
include("${PROJECT_SOURCE_DIR}/cmake/PackageDependencies.cmake")
add_library(property_codec STATIC property_codec.cpp wire_limits.cpp)
target_link_libraries(property_codec PRIVATE Rhythm::Graph project_schema protobuf::libprotobuf)
rhythm_project_target(property_codec)
add_library(runtime_package STATIC program_codec.cpp package_archive.cpp file_archive.cpp runtime_package.cpp music_package.cpp)
target_include_directories(runtime_package PUBLIC include)
target_include_directories(runtime_package PRIVATE "${PROJECT_SOURCE_DIR}/third_party/sources/picosha2")
target_link_libraries(runtime_package PUBLIC Rhythm::Graph asset_types media_types atomic_storage PRIVATE project_schema protobuf::libprotobuf
    property_codec rhythm_miniz nlohmann_json::nlohmann_json)
rhythm_project_target(runtime_package)
add_library(project_io STATIC codec.cpp graph_records.cpp graph_validation.cpp layout_codec.cpp project_store.cpp project_publish.cpp project_template.cpp async_store.cpp)
target_include_directories(project_io PUBLIC include PRIVATE "${PROJECT_SOURCE_DIR}/third_party/sources/picosha2")
target_link_libraries(project_io PUBLIC editor_application foundation_async PRIVATE project_schema protobuf::libprotobuf
    atomic_storage asset_store property_codec runtime_package prepared_assets nlohmann_json::nlohmann_json)
rhythm_project_target(project_io)
find_package(Python3 COMPONENTS Interpreter REQUIRED)
set(template_output "${PROJECT_BINARY_DIR}/content/templates/signal_texture")
file(GLOB template_manifests CONFIGURE_DEPENDS "${PROJECT_SOURCE_DIR}/content/templates/*/manifest.json")
set(template_products)
set(template_names)
foreach(manifest IN LISTS template_manifests)
    get_filename_component(source_dir "${manifest}" DIRECTORY)
    get_filename_component(template_name "${source_dir}" NAME)
    set(output_dir "${PROJECT_BINARY_DIR}/content/templates/${template_name}")
    file(GLOB template_assets CONFIGURE_DEPENDS "${source_dir}/assets/sha256/*/*")
    file(GLOB template_thumbnails CONFIGURE_DEPENDS "${source_dir}/thumbnail.rgba" "${source_dir}/thumbnail.json")
    list(APPEND template_names "${template_name}")
    list(APPEND template_products "${output_dir}/graph.pb" "${output_dir}/manifest.json" "${output_dir}/editor.json")
    add_custom_command(OUTPUT "${output_dir}/graph.pb" "${output_dir}/manifest.json" "${output_dir}/editor.json"
        COMMAND "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tools/compile-content.py"
            "${RHYTHM_PROTOC}" "${schema}" "${source_dir}" "${output_dir}"
        DEPENDS "${schema}" "${source_dir}/graph.textproto" "${manifest}"
            "${source_dir}/editor.json" ${template_thumbnails} ${template_assets} "${PROJECT_SOURCE_DIR}/tools/compile-content.py" VERBATIM)
endforeach()
add_custom_target(phase_a_content ALL DEPENDS ${template_products})
if(NOT CMAKE_CROSSCOMPILING)
    add_executable(rhythm_package package_tool.cpp)
    target_link_libraries(rhythm_package PRIVATE project_io runtime_package)
    rhythm_project_target(rhythm_package)
    set(runtime_packages)
    set(previous_package_output)
    foreach(template_name IN LISTS template_names)
        set(runtime_package_output "${PROJECT_BINARY_DIR}/content/packages/${template_name}.rhythmpack")
        set(source_dir "${PROJECT_BINARY_DIR}/content/templates/${template_name}")
        list(APPEND runtime_packages "${runtime_package_output}")
        add_custom_command(OUTPUT "${runtime_package_output}"
            COMMAND "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tools/compile-runtime-package.py"
                --publisher "$<TARGET_FILE:rhythm_package>" --sdk "${RHYTHM_IO_SDK}"
                --media-sdk "${RHYTHM_MEDIA_SDK}"
                --template "${source_dir}" --output "${runtime_package_output}"
            DEPENDS rhythm_package "${source_dir}/graph.pb" "${source_dir}/manifest.json"
                "${PROJECT_SOURCE_DIR}/tools/compile-runtime-package.py" ${previous_package_output} VERBATIM)
        # Atomic publication intentionally has one writer per output directory.
        set(previous_package_output "${runtime_package_output}")
    endforeach()
    add_custom_target(runtime_builtin_content ALL DEPENDS ${runtime_packages})
endif()
if(BUILD_TESTING)
    add_executable(file_archive_tests tests/file_archive_tests.cpp)
    target_include_directories(file_archive_tests PRIVATE . "${PROJECT_SOURCE_DIR}/third_party/sources/picosha2")
    target_link_libraries(file_archive_tests PRIVATE runtime_package atomic_storage nlohmann_json::nlohmann_json)
    rhythm_project_target(file_archive_tests)
    add_test(NAME file_archive COMMAND file_archive_tests "${PROJECT_BINARY_DIR}/file-archive-tests")
    add_executable(soundtrack_contract_tests tests/soundtrack_contract_tests.cpp)
    target_include_directories(soundtrack_contract_tests PRIVATE .)
    target_link_libraries(soundtrack_contract_tests PRIVATE project_io runtime_package asset_store nlohmann_json::nlohmann_json)
    rhythm_project_target(soundtrack_contract_tests)
    add_test(NAME soundtrack_contracts COMMAND soundtrack_contract_tests "${PROJECT_BINARY_DIR}/soundtrack-tests")
    if(WIN32)
        set_tests_properties(soundtrack_contracts PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
    add_executable(asset_reference_tests tests/asset_reference_tests.cpp)
    target_link_libraries(asset_reference_tests PRIVATE project_io project_schema protobuf::libprotobuf)
    rhythm_project_target(asset_reference_tests)
    add_test(NAME asset_references COMMAND asset_reference_tests)
    if(WIN32)
        set_tests_properties(asset_references PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
    add_executable(component_io_tests tests/component_io_tests.cpp)
    target_link_libraries(component_io_tests PRIVATE project_io runtime_package project_schema protobuf::libprotobuf)
    rhythm_project_target(component_io_tests)
    add_test(NAME component_io COMMAND component_io_tests)
    if(WIN32)
        set_tests_properties(component_io PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
    add_executable(template_contract_tests tests/template_contract_tests.cpp)
    target_link_libraries(template_contract_tests PRIVATE project_io runtime_package graph_runtime)
    rhythm_project_target(template_contract_tests)
    add_dependencies(template_contract_tests phase_a_content)
    add_test(NAME template_contracts COMMAND template_contract_tests "${PROJECT_BINARY_DIR}/content/templates")
    if(WIN32)
        set_tests_properties(template_contracts PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
    add_executable(wire_contract_tests tests/wire_contract_tests.cpp wire_limits.cpp)
    target_include_directories(wire_contract_tests PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
    rhythm_project_target(wire_contract_tests)
    add_test(NAME wire_contracts COMMAND wire_contract_tests)
    add_executable(package_contract_tests tests/package_contract_tests.cpp)
    target_include_directories(package_contract_tests PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
    target_link_libraries(package_contract_tests PRIVATE runtime_package nlohmann_json::nlohmann_json)
    rhythm_project_target(package_contract_tests)
    add_test(NAME package_contracts COMMAND package_contract_tests "${PROJECT_BINARY_DIR}/package-tests"
        "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures/legacy-v1.rhythmpack")
    if(WIN32)
        set_tests_properties(package_contracts PROPERTIES
            ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
    add_executable(program_contract_tests tests/program_contract_tests.cpp)
    target_link_libraries(program_contract_tests PRIVATE runtime_package graph_runtime project_schema protobuf::libprotobuf)
    rhythm_project_target(program_contract_tests)
    add_test(NAME program_contracts COMMAND program_contract_tests)
    if(WIN32)
        set_tests_properties(program_contracts PROPERTIES
            ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
    add_executable(persistence_contract_tests tests/persistence_contract_tests.cpp)
    target_link_libraries(persistence_contract_tests PRIVATE project_io runtime_package asset_store protobuf::libprotobuf project_schema)
    rhythm_project_target(persistence_contract_tests)
    add_dependencies(persistence_contract_tests phase_a_content)
    add_test(NAME persistence_contracts COMMAND persistence_contract_tests "${template_output}" "${PROJECT_BINARY_DIR}/test-projects")
    if(WIN32)
        set_tests_properties(persistence_contracts PROPERTIES
            ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
endif()
