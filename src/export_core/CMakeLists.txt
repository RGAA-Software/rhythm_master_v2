add_library(export_types INTERFACE)
target_include_directories(export_types INTERFACE include "${PROJECT_SOURCE_DIR}/src/media/include")
add_library(export_core STATIC render_export.cpp soundtrack.cpp encoding_queue.cpp)
target_include_directories(export_core PUBLIC include)
target_link_libraries(export_core PUBLIC export_types media_av_writer project_io Rhythm::Render
    PRIVATE graph_runtime video_sources audio_analysis media_mixer)
rhythm_project_target(export_core)
if(BUILD_TESTING)
    add_executable(export_soundtrack_tests tests/soundtrack_tests.cpp soundtrack.cpp)
    target_include_directories(export_soundtrack_tests PRIVATE .)
    target_link_libraries(export_soundtrack_tests PRIVATE media_mixer audio_analysis)
    rhythm_project_target(export_soundtrack_tests)
    add_test(NAME export_soundtrack COMMAND export_soundtrack_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests/tone.flac")
    set_tests_properties(export_soundtrack PROPERTIES FIXTURES_REQUIRED local_audio_media TIMEOUT 30)
    if(WIN32)
        set_tests_properties(export_soundtrack PROPERTIES ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
endif()
