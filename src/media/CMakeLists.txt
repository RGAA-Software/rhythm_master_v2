set(RHYTHM_MEDIA_SDK "" CACHE PATH "Installed vcpkg target-triplet directory")
if(NOT EXISTS "${RHYTHM_MEDIA_SDK}/include/libavcodec/avcodec.h")
    message(FATAL_ERROR "Set RHYTHM_MEDIA_SDK to an installed vcpkg FFmpeg triplet")
endif()
add_library(media_audio STATIC audio_decoder.cpp local_input.cpp)
add_library(media_waveform STATIC waveform.cpp waveform_index.cpp waveform_cache.cpp)
target_include_directories(media_waveform PUBLIC include)
target_link_libraries(media_waveform PUBLIC asset_types parameters PRIVATE media_audio foundation_async asset_store)
rhythm_project_target(media_waveform)
target_include_directories(media_audio PUBLIC include)
target_include_directories(media_audio SYSTEM PRIVATE "${RHYTHM_MEDIA_SDK}/include")
foreach(component avformat avcodec swresample avutil swscale)
    find_library(RHYTHM_MEDIA_${component} NAMES ${component}
        PATHS "${RHYTHM_MEDIA_SDK}/lib" NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH REQUIRED)
    if(WIN32)
        file(GLOB component_dll "${RHYTHM_MEDIA_SDK}/bin/${component}-*.dll")
        list(LENGTH component_dll component_dll_count)
        if(NOT component_dll_count EQUAL 1)
            message(FATAL_ERROR "Expected one validated ${component} DLL in the media SDK")
        endif()
        add_library(media_backend_${component} SHARED IMPORTED GLOBAL)
        get_filename_component(component_dll_name "${component_dll}" NAME)
        set_target_properties(media_backend_${component} PROPERTIES
            IMPORTED_IMPLIB "${RHYTHM_MEDIA_${component}}" IMPORTED_LOCATION "${component_dll}"
            IMPORTED_IMPLIB_DEBUG "${RHYTHM_MEDIA_SDK}/debug/lib/${component}.lib"
            IMPORTED_LOCATION_DEBUG "${RHYTHM_MEDIA_SDK}/debug/bin/${component_dll_name}")
    else()
        add_library(media_backend_${component} UNKNOWN IMPORTED GLOBAL)
        set_target_properties(media_backend_${component} PROPERTIES
            IMPORTED_LOCATION "${RHYTHM_MEDIA_${component}}")
    endif()
endforeach()
target_link_libraries(media_audio PUBLIC atomic_storage PRIVATE media_backend_avformat media_backend_avcodec
    media_backend_swresample media_backend_avutil)
if(WIN32)
    add_library(media_backend_zlib SHARED IMPORTED GLOBAL)
    set_target_properties(media_backend_zlib PROPERTIES
        IMPORTED_IMPLIB "${RHYTHM_MEDIA_SDK}/lib/zlib.lib"
        IMPORTED_LOCATION "${RHYTHM_MEDIA_SDK}/bin/zlib1.dll"
        IMPORTED_IMPLIB_DEBUG "${RHYTHM_MEDIA_SDK}/debug/lib/zlibd.lib"
        IMPORTED_LOCATION_DEBUG "${RHYTHM_MEDIA_SDK}/debug/bin/zlibd1.dll")
    target_link_libraries(media_audio PRIVATE media_backend_zlib)
endif()
if(ANDROID)
    find_library(RHYTHM_MEDIA_z NAMES z zlib PATHS "${RHYTHM_MEDIA_SDK}/lib"
        NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH REQUIRED)
    target_link_libraries(media_audio PRIVATE android m atomic "${RHYTHM_MEDIA_z}")
endif()
rhythm_project_target(media_audio)
add_library(media_mixer STATIC audio_mixer.cpp audio_cursor_budget.cpp crossfade.cpp)
target_include_directories(media_mixer PUBLIC include)
target_link_libraries(media_mixer PUBLIC media_audio media_arrangement)
rhythm_project_target(media_mixer)
add_library(media_video STATIC video_decoder.cpp)
target_include_directories(media_video PUBLIC include)
target_include_directories(media_video SYSTEM PRIVATE "${RHYTHM_MEDIA_SDK}/include")
target_link_libraries(media_video PUBLIC media_types PRIVATE media_audio media_backend_swscale)
rhythm_project_target(media_video)
add_library(media_av_writer STATIC av_writer.cpp local_output.cpp)
target_include_directories(media_av_writer PUBLIC include)
target_include_directories(media_av_writer SYSTEM PRIVATE "${RHYTHM_MEDIA_SDK}/include")
target_link_libraries(media_av_writer PRIVATE media_audio media_backend_swscale)
rhythm_project_target(media_av_writer)
if(BUILD_TESTING)
    add_executable(crossfade_tests tests/crossfade_tests.cpp)
    target_link_libraries(crossfade_tests PRIVATE media_mixer)
    rhythm_project_target(crossfade_tests)
    add_test(NAME crossfade COMMAND crossfade_tests)
    if(WIN32)
        set_tests_properties(crossfade PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
    add_executable(audio_mixer_tests tests/audio_mixer_tests.cpp)
    target_link_libraries(audio_mixer_tests PRIVATE media_mixer)
    rhythm_project_target(audio_mixer_tests)
    add_test(NAME audio_mixer COMMAND audio_mixer_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests")
    set_tests_properties(audio_mixer PROPERTIES FIXTURES_REQUIRED local_audio_media TIMEOUT 60)
    if(WIN32)
        set_tests_properties(audio_mixer PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
    add_executable(waveform_tests tests/waveform_tests.cpp)
    target_link_libraries(waveform_tests PRIVATE media_waveform media_audio foundation_async)
    rhythm_project_target(waveform_tests)
    add_test(NAME waveform COMMAND waveform_tests "${PROJECT_BINARY_DIR}/large-music-fixtures/resonance_demo.wav")
    set_tests_properties(waveform PROPERTIES FIXTURES_REQUIRED large_music_pcm TIMEOUT 60)
    if(WIN32)
        set_tests_properties(waveform PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
    add_executable(media_av_writer_tests tests/av_writer_tests.cpp)
    target_link_libraries(media_av_writer_tests PRIVATE media_av_writer media_video)
    rhythm_project_target(media_av_writer_tests)
    add_test(NAME media_av_writer COMMAND media_av_writer_tests "${PROJECT_BINARY_DIR}/av-writer-tests")
    if(WIN32)
        set_tests_properties(media_av_writer PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin;PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/debug/bin")
    endif()
    add_executable(media_audio_tests tests/audio_decoder_tests.cpp tests/compressed_fixture.cpp)
    target_include_directories(media_audio_tests SYSTEM PRIVATE "${RHYTHM_MEDIA_SDK}/include")
    target_link_libraries(media_audio_tests PRIVATE media_audio)
    rhythm_project_target(media_audio_tests)
    add_test(NAME media_audio COMMAND media_audio_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests")
    set_tests_properties(media_audio PROPERTIES FIXTURES_SETUP local_audio_media)
    if(WIN32)
        set_tests_properties(media_audio PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin;PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/debug/bin")
    endif()
    add_executable(media_video_tests tests/video_decoder_tests.cpp)
    target_link_libraries(media_video_tests PRIVATE media_video)
    rhythm_project_target(media_video_tests)
endif()
