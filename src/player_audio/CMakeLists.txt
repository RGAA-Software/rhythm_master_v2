add_library(player_audio STATIC scene_audio_bridge.cpp)
target_include_directories(player_audio PUBLIC include)
target_link_libraries(player_audio PUBLIC player_core PRIVATE audio_playback)
rhythm_project_target(player_audio)
if(BUILD_TESTING)
    add_executable(scene_audio_bridge_tests tests/scene_audio_bridge_tests.cpp)
    target_link_libraries(scene_audio_bridge_tests PRIVATE player_audio audio_playback asset_store)
    rhythm_project_target(scene_audio_bridge_tests)
    add_test(NAME scene_audio_bridge COMMAND scene_audio_bridge_tests "${PROJECT_BINARY_DIR}/audio-decoder-tests")
    set_tests_properties(scene_audio_bridge PROPERTIES FIXTURES_REQUIRED local_audio_media TIMEOUT 30)
    if(WIN32)
        set_tests_properties(scene_audio_bridge PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_MEDIA_SDK}/bin")
    endif()
endif()
