add_subdirectory(../audio_output "${PROJECT_BINARY_DIR}/src/audio_output")
if(RHYTHM_BUILD_MEDIA)
    add_subdirectory(../audio_playback "${PROJECT_BINARY_DIR}/src/audio_playback")
endif()
add_library(rhythm_android SHARED main.cpp host.cpp commands.cpp package_imports.cpp)
set_target_properties(rhythm_android PROPERTIES OUTPUT_NAME main)
target_include_directories(rhythm_android PRIVATE "${PROJECT_SOURCE_DIR}/src/rhythm_render/src")
target_link_libraries(rhythm_android PRIVATE player_core render_bgfx SDL3::SDL3-shared android log)
rhythm_project_target(rhythm_android)
if(RHYTHM_BUILD_MEDIA)
    target_sources(rhythm_android PRIVATE music_playback.cpp)
    target_link_libraries(rhythm_android PRIVATE audio_playback)
    target_compile_definitions(rhythm_android PRIVATE RHYTHM_HAS_LOCAL_MEDIA=1)
endif()
find_package(Python3 REQUIRED COMPONENTS Interpreter)
set(RHYTHM_PLAYER_PACKAGE "${PROJECT_SOURCE_DIR}/out/windows/content/packages/signal_texture.rhythmpack"
    CACHE FILEPATH "Published package produced by the native host compiler")
set(android_package_command "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tools/build-android-player.py"
    --skip-native --package "${RHYTHM_PLAYER_PACKAGE}" --build "${PROJECT_BINARY_DIR}"
    --configuration "${CMAKE_BUILD_TYPE}")
add_custom_command(TARGET rhythm_android POST_BUILD COMMAND ${android_package_command} VERBATIM)
add_custom_target(android_player_apk ALL COMMAND ${android_package_command} DEPENDS rhythm_android VERBATIM)
if(BUILD_TESTING)
    add_executable(android_gpu_contract_tests tests/gpu_contract_tests.cpp tests/point_gpu.cpp tests/effect_gpu.cpp tests/template_gpu.cpp tests/video_upload_gpu.cpp tests/texture_reuse_gpu.cpp)
    target_sources(android_gpu_contract_tests PRIVATE ../rhythm_render/tests/readback_gpu_contracts.cpp)
    target_include_directories(android_gpu_contract_tests PRIVATE "${PROJECT_SOURCE_DIR}/src/runtime")
    target_include_directories(android_gpu_contract_tests PRIVATE "${PROJECT_SOURCE_DIR}/src/runtime/tests")
    target_include_directories(android_gpu_contract_tests PRIVATE "${PROJECT_SOURCE_DIR}/src/rhythm_render/src")
    target_include_directories(android_gpu_contract_tests PRIVATE "${PROJECT_SOURCE_DIR}/src/rhythm_render/tests")
    target_link_libraries(android_gpu_contract_tests PRIVATE render_bgfx Rhythm::Render player_core EGL GLESv3 gpu_execution_probe)
    if(RHYTHM_BUILD_MEDIA)
        target_sources(android_gpu_contract_tests PRIVATE tests/soundtrack_gpu.cpp)
        target_link_libraries(android_gpu_contract_tests PRIVATE media_audio audio_analysis)
        target_compile_definitions(android_gpu_contract_tests PRIVATE RHYTHM_HAS_LOCAL_MEDIA=1)
    endif()
    rhythm_project_target(android_gpu_contract_tests)
endif()
