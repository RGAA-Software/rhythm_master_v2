add_library(parameters STATIC curve.cpp expression.cpp controls.cpp control_sequence.cpp clip_interval.cpp beat_grid.cpp events.cpp)
target_sources(parameters PRIVATE event_envelope.cpp event_track.cpp)
target_include_directories(parameters PUBLIC include)
rhythm_project_target(parameters)
if(BUILD_TESTING)
    add_executable(event_track_tests tests/event_track_tests.cpp)
    target_link_libraries(event_track_tests PRIVATE parameters)
    rhythm_project_target(event_track_tests)
    add_test(NAME event_track COMMAND event_track_tests)
    add_executable(event_envelope_tests tests/event_envelope_tests.cpp)
    target_link_libraries(event_envelope_tests PRIVATE parameters)
    rhythm_project_target(event_envelope_tests)
    add_test(NAME event_envelope COMMAND event_envelope_tests)
    add_executable(event_tests tests/event_tests.cpp)
    target_link_libraries(event_tests PRIVATE parameters)
    rhythm_project_target(event_tests)
    add_test(NAME events COMMAND event_tests)
    add_executable(beat_grid_tests tests/beat_grid_tests.cpp)
    target_link_libraries(beat_grid_tests PRIVATE parameters)
    rhythm_project_target(beat_grid_tests)
    add_test(NAME beat_grid COMMAND beat_grid_tests)
    add_executable(clip_interval_tests tests/clip_interval_tests.cpp)
    target_link_libraries(clip_interval_tests PRIVATE parameters)
    rhythm_project_target(clip_interval_tests)
    add_test(NAME clip_interval COMMAND clip_interval_tests)
    add_executable(control_sequence_tests tests/control_sequence_tests.cpp)
    target_link_libraries(control_sequence_tests PRIVATE parameters)
    rhythm_project_target(control_sequence_tests)
    add_test(NAME control_sequence COMMAND control_sequence_tests)
    add_executable(control_contract_tests tests/control_contract_tests.cpp)
    target_link_libraries(control_contract_tests PRIVATE parameters)
    rhythm_project_target(control_contract_tests)
    add_test(NAME control_contracts COMMAND control_contract_tests)
    add_executable(expression_tests tests/expression_tests.cpp)
    target_link_libraries(expression_tests PRIVATE parameters)
    rhythm_project_target(expression_tests)
    add_test(NAME expression COMMAND expression_tests)
    add_executable(curve_contract_tests tests/curve_contract_tests.cpp)
    target_link_libraries(curve_contract_tests PRIVATE parameters)
    rhythm_project_target(curve_contract_tests)
    add_test(NAME curve_contracts COMMAND curve_contract_tests)
endif()
