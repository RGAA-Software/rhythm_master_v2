add_library(parameters STATIC curve.cpp expression.cpp controls.cpp)
target_include_directories(parameters PUBLIC include)
rhythm_project_target(parameters)
if(BUILD_TESTING)
    add_executable(control_contract_tests tests/control_contract_tests.cpp)
    target_link_libraries(control_contract_tests PRIVATE parameters)
    rhythm_project_target(control_contract_tests)
    add_test(NAME control_contracts COMMAND control_contract_tests)
    add_executable(expression_tests tests/expression_tests.cpp)
    target_link_libraries(expression_tests PRIVATE parameters)
    rhythm_project_target(expression_tests)
    add_test(NAME expression COMMAND expression_tests)
    add_executable(curve_contract_tests tests/curve_contract_tests.cpp)
    target_link_libraries(curve_contract_tests PRIVATE parameters)
    rhythm_project_target(curve_contract_tests)
    add_test(NAME curve_contracts COMMAND curve_contract_tests)
endif()
