add_library(atomic_storage STATIC atomic_file.cpp)
target_include_directories(atomic_storage PUBLIC include)
rhythm_project_target(atomic_storage)
if(BUILD_TESTING)
    add_executable(publication_tests tests/publication_tests.cpp)
    target_link_libraries(publication_tests PRIVATE atomic_storage)
    rhythm_project_target(publication_tests)
    add_test(NAME publication COMMAND publication_tests "${PROJECT_BINARY_DIR}/publication-tests")
endif()
