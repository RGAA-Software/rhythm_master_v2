add_library(cluster_player STATIC playback_schedule.cpp)
target_include_directories(cluster_player PUBLIC include)
target_link_libraries(cluster_player PUBLIC player_core cluster_scene)
rhythm_project_target(cluster_player)
if(BUILD_TESTING)
    add_executable(schedule_contract_tests tests/schedule_contract_tests.cpp)
    target_link_libraries(schedule_contract_tests PRIVATE cluster_player)
    rhythm_project_target(schedule_contract_tests)
    add_test(NAME schedule_contracts COMMAND schedule_contract_tests)
    if(WIN32)
        set_tests_properties(schedule_contracts PROPERTIES ENVIRONMENT_MODIFICATION
            "PATH=path_list_prepend:${RHYTHM_IO_SDK}/bin;PATH=path_list_prepend:${RHYTHM_IO_SDK}/debug/bin")
    endif()
endif()
