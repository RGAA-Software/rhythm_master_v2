add_library(foundation_async STATIC blocking_executor.cpp thread_joiner.cpp)
target_include_directories(foundation_async PUBLIC include)
rhythm_project_target(foundation_async)
if(BUILD_TESTING)
    add_executable(async_contract_tests tests/async_contract_tests.cpp)
    target_link_libraries(async_contract_tests PRIVATE foundation_async)
    rhythm_project_target(async_contract_tests)
    add_test(NAME async_contracts COMMAND async_contract_tests)
    set_tests_properties(async_contracts PROPERTIES TIMEOUT 30)
endif()
